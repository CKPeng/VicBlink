# 👁️ VicBlink 完整产品演进路线图 (Roadmap & Architecture)

基于眼科界公认的 **“20-20-20” 法则**：
> 每看屏幕 20 分钟 ➔ 将视线移开，看向 20 英尺（约 6 米）外的远方 ➔ 保持至少 20 秒。

本项目采用 **Tauri v2 (Rust) + React 18 + TypeScript + Tailwind CSS** 架构，现已完整实现三个阶段的全部核心机制与扩展能力：

---

## 阶段一：V1.0 核心机制（MVP：解决“能用”和“好用”的闭环） 【已完成 ✅】

- [x] **双轨状态计时器 (Core Timer)**
  - 工作周期：固定 20 分钟专注期倒计时。
  - 休息周期：固定 20 秒远眺期倒计时。
  - 状态控制：统一大写枚举（`WORKING`, `BREAK`, `PAUSED`, `IDLE`），支持即时开始、暂停、恢复、重置与“跳过本次休息”（Skip）。
- [x] **状态栏驻留与轻交互 (Menu Bar & Tray)**
  - 动态指示器：Mac 顶部菜单栏/系统托盘动态显示剩余时间（如 `20m`、`18m`，休息时显示 `20s`）。
  - 快捷面板：点击图标弹出面板，提供“暂停 1 小时 / 2 小时”（用于紧急排查 Bug 或开会）以及基础操作。
  - 测试加速模式：内置一键切换“10秒工作 + 5秒休息”，几秒内验证完整流程。
- [x] **强打断多屏幕全屏遮罩 (Multi-Monitor Full-Screen Overlay)**
  - 多屏覆盖：Rust 底层调用 `available_monitors` 检测所有主屏与外接副屏，同步拉起置顶无边框全屏毛玻璃遮罩，彻底杜绝转头看副屏的漏洞。
  - 视觉引导：20 秒呼吸动效环形进度圈，远眺引导文案。
  - 听觉引导：内置 Web Audio API 纯代码合成轻柔音效（休息开始播放空灵西藏颂钵和弦，结束播放清脆双音符），零外部音频文件依赖。
  - 逃生通道（温柔模式）：右上角显式“跳过”按钮，支持一键按 `Esc` 键退出遮罩。
- [x] **自动化云端打包 (GitHub Actions)**
  - 内置 `.github/workflows/build.yml`，推送 Tag（如 `v1.0.0`）自动利用 GitHub 免费云端 Mac 与 Windows 虚拟机生成原生的 macOS `.dmg` 与 Windows `.exe`。

---

## 阶段二：V2.0 进阶体验（打磨“无感”与“智能化”） 【已完成 ✅】

- [x] **系统闲置检测 (Idle Detection - 核心护城河)**
  - 监听用户键鼠全局活跃度。离开工位超过 3 分钟自动挂起进入 `IDLE` 暂停状态；重返工位敲击键盘或移动鼠标时，自动唤醒并恢复专注计时。
  - 彻底避免“人离开工位开会/喝水，电脑对着空气弹遮罩”的尴尬。
- [x] **“温柔 / 严厉”双模式控制 (Strict Mode)**
  - 温柔模式：遮罩右上角带有跳过按钮，支持按 `Esc` 快速退出，适合紧急线上故障应急。
  - 严厉模式：隐藏退出按钮，彻底拦截并屏蔽键盘 `Esc` 退出按键，倒计时未归零前不可关闭，强制休息满 20 秒。
- [x] **智能防打扰 (Focus / DND Integration)**
  - 会议免打扰开关：开启后，20 秒远眺期不弹全屏强打断遮罩，而是平滑降级为系统静默通知。
- [x] **全局快捷键 (Global Hotkeys)**
  - 支持键盘快捷键一键暂停/恢复专注（`Alt + P`），以及一键立即开启 20 秒远眺（`Alt + B`）。
- [x] **数据统计与正向反馈 (Analytics & Heatmap)**
  - GitHub Commit 风格的 35 天连续打卡绿色热力方块图；
  - 统计今日远眺次数、累计护眼时长、护眼成就段位徽章（护眼新秀 ➔ 睫状肌守卫 ➔ 远眺宗师）。

---

## 阶段三：V3.0+ 扩展潜力（深度融合与场景化生态） 【已完成 ✅】

- [x] **番茄钟深度融合 (Deep Work & Pomodoro Fusion)**
  - 模式自由切换：纯粹 20-20-20 护眼模式 vs 经典番茄工作法（50 分钟工作 + 10 分钟大休息）。
  - **首创“双轨嵌套微远眺”**：在 50 分钟深度专注周期中，可选每 20 分钟轻量内嵌一次 20 秒微远眺，长短结合，兼顾深度工作心流与视力保护。
- [x] **场景化沉浸式白噪音 (Ambient Soundscapes)**
  - 基于 Web Audio API 纯算法合成 4 种原生态环境音效：**海浪微风 (Ocean Waves)**、**山间晨雨 (Gentle Rain)**、**林间风声 (Forest Breeze)**、**壁炉柴火 (Campfire)**。
  - 遮罩拉起时 1.5 秒平滑淡入，倒计时结束前 1.0 秒平滑淡出，听觉极度舒缓放松。
  - 设置面板中支持现场试听与一键切换。
- [x] **统一偏好设置中心 (Settings Modal)**
  - 在面板中统一调节计时模式、严厉模式、白噪音选择与会议免打扰，配置自动本地持久化。